{
  "name": "webcam-filters-extension",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf build && NODE_ENV=production node esbuild.js",
    "start": "rm -rf build && NODE_ENV=dev node esbuild.js"
  },
  "author": "",
  "license": "ISC",
  "prettier": "prettier-config-standard",
  "devDependencies": {
    "@types/webextension-polyfill": "0.8.3",
    "esbuild": "0.14.27",
    "prettier": "2.6.0",
    "prettier-config-standard": "5.0.0",
    "typescript": "4.6.2",
    "webextension-polyfill": "0.9.0"
  },
  "dependencies": {
    "@tensorflow-models/blazeface": "0.0.7",
    "@tensorflow/tfjs-backend-webgl": "3.15.0"
  }
}
